paths:
    css_src_dir: 'static/css/src'
    css_build_dir: 'static/css/build'
    css_stylus_dir: 'static/css/styl'
    js_src_dir: 'static/js/src'
    js_build_dir: 'static/js/build'
minify:
    css:
    #    - style.css
    js:
    #    - script.js
crontabs:
    #- name: 'calling /bin/true'
    #  crontab: '* 10 * * * /bin/true'
    #- name: 'base path test'
    #  crontab: '* 20 * * * {base_path}/manage.py'
    #- name: 'current project path test'
    #  crontab: '* 20 * * * {project_path}/manage.py'
